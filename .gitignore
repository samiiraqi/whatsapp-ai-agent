# Dependencies
node_modules/

# Secrets
.env

# Local data (SQLite database, contains hashed phone numbers + message text)
app/server/data/*.db

# Terraform (the .terraform.lock.hcl provider lock file IS committed)
.terraform/
*.tfstate
*.tfstate.*

# OS files
.DS_Store
Thumbs.db
