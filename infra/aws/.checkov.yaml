# Skip list matches the "documented, not fixed" findings explained in
# infra/aws/README.md's Checkov section. Keep the two in sync.
framework:
  - terraform
skip-check:
  - CKV_AWS_309 # API Gateway routes unauthenticated - WhatsApp calls this webhook directly; verified by HMAC signature in the app
  - CKV_AWS_119 # DynamoDB customer-managed KMS key - real recurring cost, default encryption already on
  - CKV_AWS_117 # Lambda in a VPC - needs a NAT Gateway (~$32+/mo) this small webhook doesn't need
  - CKV_AWS_272 # Lambda code-signing - no real deployment pipeline yet to sign against
  - CKV_AWS_173 # Lambda env var KMS CMK - env vars hold Secrets Manager ARNs, not values; default encryption already on
  - CKV_AWS_338 # Log retention >= 1 year - this project's deliberate 14-day retention, not an oversight
  - CKV_AWS_158 # CloudWatch Log Group KMS CMK - real recurring cost, default encryption already on
  - CKV_AWS_149 # Secrets Manager KMS CMK - real recurring cost, default encryption already on
  - CKV2_AWS_57 # Secrets Manager rotation - no real secret values yet to rotate
